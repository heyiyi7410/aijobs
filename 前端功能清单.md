# 找工作助手 · 前端功能清单

> 用途：作为「重新设计一版样式」的基线。下面每一项都是当前代码里**真实存在**的功能，
>  redesign 时不要漏掉，也尽量保留已定下的交互约定（见末尾「设计约束」）。
> 产物形态：Vue3 + Vite 单页（4 步向导），无路由，靠 `store.step` 切换。

---

## 一、整体框架（App.vue）

| 功能 | 说明 |
|---|---|
| 4 步向导 | 填信息 → 选单位 → 挑岗位 → 去投递，StepBar 可点回已走过的步 |
| 顶部栏 | 标题「找工作助手」+ 两句副标题 + 两个常驻按钮：字号 A+/A-、朗读当前步说明 |
| 全局错误条 | 底部居中红字 `store.err` |
| 等待提示 | 第 2 步查岗时显示「正在一家一家去单位网站找岗位…已跑了 N 秒」 |
| 底部导航 | 上一步 / 下一步（按钮文案随步变化，加载时显示「正在找…」） |

**重设计注意**：顶部两个小按钮（字号、朗读）是全局可见的，样式要稳定不抢戏。

---

## 二、第 1 步 · 填信息（Step1Profile.vue）

| 功能 | 说明 |
|---|---|
| 简历上传解析 | 选 PDF/Word/txt/md → 后端读成字段 → 自动填表，并标「简历读到的 / 核对一下」标记 |
| 保留原件开关 | 「留着这份简历，自动投直接用原件」（带照片排版）；否则用文字拼一页 |
| 读取结果回显 | 列出从简历读到的 N 项 key/value，可疑项标黄提醒核对 |
| 基础信息表单 | 姓名*、手机号*、邮箱*、年龄、性别、出生年月、学历、学校、专业、毕业时间（必填* 才校验） |
| 国聘必填段 | 身高、体重、紧急联系人姓名/电话、通信地址（国聘基本信息段缺了保存过不去） |
| 仅本人可知项 | 民族、婚姻、学历性质（统招/非统招）、学习形式、学位证、外语及等级、服从调剂、健康——**绝不替用户编**，不填则留空并提示补 |
| 结构化经历编辑器 | 工作经历 / 实习 / 校内活动 / 教育，可增删；含公司、职位、起止、描述（教育含学历） |
| 技能多选 | 30+ 技能 chips（会开车、电工证、普通话好…），可点选 |
| 自由描述 | 文本框「还想说说什么」 |
| 语音输入 | 姓名、自由描述两处支持🎤语音填充（Web Speech API） |

**重设计注意**：表单极长，是整站最长的屏。chips（标签按钮）是核心交互元件， redesign 要把 chips 的视觉层级、选中态、点击反馈做扎实。

---

## 三、第 2 步 · 选单位（Step2Wish.vue）

| 功能 | 说明 |
|---|---|
| 单位性质 | 7 类 chips：央企/国企/事业单位/公务员/外企/合资/民营（默认勾前 6 类，含民营） |
| 用工方式 | 直签 / 都可以（劳务派遣也可接受） |
| 想做什么工作 | 13 类工种 chips 多选 + 「上面没有？自己填关键词」输入框 |
| 城市 | 32 城 chips + 「不限城市」+ 手填其他城市 |
| 全国/本省一起搜 | 默认勾选（国企公告多写「全国/省名」），取消=只要本市 |
| 期望月薪 | 5 档 chips：3000以下 ~ 12000以上 |

**重设计注意**：这步也是 chips 为主，和第 1 步的 chips 应保持同一套视觉语言。

---

## 四、第 3 步 · 挑岗位（Step3Match.vue）

| 功能 | 说明 |
|---|---|
| 岗位列表 | 按适合程度排序的岗位卡片，点圆圈选中（整卡可点） |
| 适配合成 | 岗位徽章：性质（央企/国企/…）、直签/劳务派遣、招聘类型、可代投（有邮箱）、可电话联系、全国招聘/本省/外地 |
| 匹配度展示 | 分数 + 等级（很适合/较适合…）+ 适合原因列表 |
| 岗位详情展开 | 职责/公告摘要、报名时间、招聘人数、报名邮箱、联系电话（带拨号）、原公告链接 |
| 直达链接 | 卡片上的「直达」按钮，新窗打开对方报名/公告页，小字显示域名 |
| 按单位名搜索 | 顶部搜索框，敲单位名（如「武钢」）实时拉该家岗位 |
| 筛选 chips | 全部 / 只看很适合（≥70） / 只看能代投 / 本市·外地切换 / 全部选择·取消全选 |
| 手动抓取更多 | 滑到底显示提示 → 继续下拉触发实时抓取（force），合并新岗进列表 |
| 状态提示条 | 来源渠道、本市/外地补全说明、全国招聘提醒、已剔除失效岗位、离线兜底示例 |
| 进度/空态 | 已选中计数；无结果时按筛选给不同解释文案 |

**重设计注意**：这是信息密度最高的一屏，也是用户停留最久的屏。卡片布局、徽章配色、筛选条是视觉重点。宽屏两列、手机单列的响应式要保留。

---

## 五、第 4 步 · 去投递（Step4Apply.vue + AutoFillPanel.vue）

### 5.1 分组（核心交互）
| 功能 | 说明 |
|---|---|
| 两组拆分 | 按「公告有无报名邮箱」劈成：① 能代投（有邮箱）② 要自己去官网投（无邮箱） |
| 组①代投按钮 | 「一键代投这 N 个」（数量写在按钮上）；没设邮箱时引导去设置 |
| 授权码告警 | 已存授权码可疑时整组常驻提醒「授权码可能不对（邮箱没错）」 |
| 组②官网投 | 「开始逐个投递 N 个」，配合自动填表 |

### 5.2 发件邮箱配置弹窗
| 功能 | 说明 |
|---|---|
| 常驻入口 | 标题栏「设置发件邮箱」按钮，与内容无关地常驻 |
| 邮箱+授权码 | 填邮箱地址 + 授权码（非登录密码），autocomplete=new-password 防误填 |
| 微软系识别 | 手填 Outlook/Hotmail 时当场提示「不能用密码代发，改用网页发送」 |
| 测一测 | 真连一次服务器登录（不发信）验证授权码对不对 |
| 未保存提醒 | 改了没保存时拦截关弹窗 |
| 各邮箱开通说明 | 自动列出 QQ/163/126/139/189/Outlook 等的 SMTP 怎么开、分步说明、网页版链接 |

### 5.3 按岗位定制简历（AI，最新功能）
| 功能 | 说明 |
|---|---|
| 配置 AI 弹窗 | 填 API 地址 / Key / 模型（OpenAI 兼容，如 Agnes） |
| 定制开关 | 组①内「按岗位定制简历」开关 |
| 只读预览 | 打开即拉每个岗位的「原文 → 改成 → 为什么」对照 + 缺口 + 事实门结果 |
| 生成 N 份 | 「生成 N 份定制简历」→ 后端渲染 docx → 一键代投时当附件发出 |
| 事实门 | 未过事实核对的岗位退回原件，绝不发编造内容 |

### 5.4 逐个投递（用自己邮箱发）
| 功能 | 说明 |
|---|---|
| 邮箱选择器弹窗 | 挑一个常用邮箱 → 打开其写信页，收件人/主题/正文三块各带「复制」 |
| 邮件 App 打开 | 手机上用系统邮件 App（内容预填好） |
| 逐项推进 | 一封一封过，下一封/算了这一家，记回执 |

### 5.5 代发进度与结果
| 功能 | 说明 |
|---|---|
| 进度条 | WebSocket 实时进度（含当前处理谁、每封间隔、预计总耗时） |
| 失败如实呈现 | 一封没发出去→整块染红；部分失败→显示成功/失败数；早停说明剩余未试 |
| 结果清单 | 每封 标题·单位·状态 |

### 5.6 自动填表（测试版，AutoFillPanel.vue）
| 功能 | 说明 |
|---|---|
| 补资料前置 | 开跑前先补 姓名/手机/邮箱/性别/出生/学校/专业/毕业时间 等空项 |
| 自定义链接 | 可粘贴任意招聘投递页替你填 |
| 真人介入点 | 短信/图形验证码、最后提交需本人；站点标红必填段列出「为什么不能替补 + 怎么补最快」，一键「无XX」声明需本人确认 |
| 过程可视化 | 实时步骤清单、替你填好的项、要你来的项、当前页截图、回执编号 |
| 浏览器可见开关 | 打开浏览器窗口看它操作（慢但安心）；重填开关 |

---

## 六、跨步骤 / 全局能力

| 功能 | 说明 | 所在 |
|---|---|---|
| 大字号开关 | A+/A- 切换 18px↔23px 根字号（长者友好） | App.vue + style.css |
| 朗读说明 | 顶部喇叭按钮朗读当前步引导语 | useSpeech.js |
| 语音输入 | 姓名/自由描述语音填充 | useSpeech.js |
| 实时进度通道 | WebSocket 收投递进度，断线自动降级轮询 | api.js initSocket |
| 跨设备复制 | 复制用 navigator.clipboard，非安全上下文降级 execCommand | Step4 |
| 设计令牌 | CSS 变量：缓动曲线、时长、语义色、圆角 | style.css |

---

## 七、重设计时必须保留的约束（来自 DESIGN.md / style.css）

1. **触屏 hover 门控**：所有 `:hover` 必须包在 `@media (hover:hover) and (pointer:fine)`，否则手机上点过就粘住悬停态。
2. **按下反馈**：按钮 `:active { transform: scale(0.97) }`，时长 token `--t-press`。
3. **prefers-reduced-motion**：只保留颜色/透明度过渡，去掉位移/缩放/旋转（加载圈可放慢保留）。
4. **overscroll-behavior: contain**：弹窗/抽屉内部滚动别带动整页。
5. **入场动画**：弹窗用 `@starting-style` 原生进场，只动 opacity/transform（GPU）。
6. **大字号瞬时生效**：字号切换不应有过渡，避免长者眩晕。
7. **禁止 emoji 当图标**：图标用内联 SVG（喇叭、麦克风等已用 SVG）。
8. **事实不编造**：UI 文案不得替用户假设（如「英语熟练」），缺失即留空提示补。

---

## 八、建议 redesign 的视觉重点排序

1. **chips 系统**（第1/2步 + 第3步筛选）：全站最高频元件，统一选中态/反馈。
2. **岗位卡片**（第3步）：信息密度高，徽章配色 + 布局是脸面。
3. **弹窗体系**（邮箱配置/AI配置/邮箱选择器）：3 类弹窗统一骨架与层级。
4. **代投分组 + 进度**（第4步）：成败状态要一眼分清（绿/红）。
5. **自动填表面板**：过程可视化，真人介入点要醒目。
6. **顶部栏 + StepBar**：全局导航稳定感。
